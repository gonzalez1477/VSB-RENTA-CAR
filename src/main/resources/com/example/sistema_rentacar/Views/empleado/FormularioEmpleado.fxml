<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.geometry.Insets?>
<?import javafx.scene.control.*?>
<?import javafx.scene.layout.*?>

<VBox xmlns="http://javafx.com/javafx/17" xmlns:fx="http://javafx.com/fxml/1"
      fx:controller="com.example.sistema_rentacar.Controllers.Empleado.FormularioEmpleadoController"
      spacing="0" prefWidth="600" prefHeight="700"
      styleClass="form-container"
      style="-fx-background-color: #f8f9fa;"
      stylesheets="@../../css/estilosFormulario.css">

    <!-- Header del formulario -->
    <HBox alignment="CENTER_LEFT" spacing="15" styleClass="form-header"
          style="-fx-background-color: linear-gradient(to right, #2c3e50, #34495e); -fx-padding: 25 30;">
        <Label text="👤" style="-fx-font-size: 32px;"/>
        <VBox spacing="3">
            <Label fx:id="lblTitulo" text="Nuevo Empleado"
                   style="-fx-font-size: 24px; -fx-font-weight: bold; -fx-text-fill: white;"/>
            <Label text="Complete la información del empleado"
                   style="-fx-font-size: 13px; -fx-text-fill: rgba(255, 255, 255, 0.8);"/>
        </VBox>
    </HBox>

    <!-- Contenido del formulario -->
    <ScrollPane fitToWidth="true" VBox.vgrow="ALWAYS"
                style="-fx-background-color: transparent; -fx-background: transparent;">
        <VBox spacing="25" style="-fx-padding: 30;">

            <!-- Sección: Información Personal -->
            <VBox spacing="15" styleClass="form-section">
                <HBox alignment="CENTER_LEFT" spacing="10" styleClass="section-header">
                    <Label text="📋" style="-fx-font-size: 18px;"/>
                    <Label text="Información Personal"
                           style="-fx-font-size: 16px; -fx-font-weight: bold; -fx-text-fill: #2c3e50;"/>
                    <Region HBox.hgrow="ALWAYS"/>
                    <Region style="-fx-min-width: 50; -fx-background-color: linear-gradient(to right, #3498db, transparent); -fx-min-height: 2;"/>
                </HBox>

                <GridPane hgap="20" vgap="18" styleClass="form-grid">

                    <!-- Nombre -->
                    <VBox spacing="5" GridPane.columnIndex="0" GridPane.rowIndex="0">
                        <Label text="Nombre *" styleClass="field-label"/>
                        <TextField fx:id="txtNombre" promptText="Ingrese el nombre"
                                   styleClass="form-field"/>
                    </VBox>

                    <!-- Apellido -->
                    <VBox spacing="5" GridPane.columnIndex="1" GridPane.rowIndex="0">
                        <Label text="Apellido *" styleClass="field-label"/>
                        <TextField fx:id="txtApellido" promptText="Ingrese el apellido"
                                   styleClass="form-field"/>
                    </VBox>

                    <!-- Email -->
                    <VBox spacing="5" GridPane.columnIndex="0" GridPane.rowIndex="1" GridPane.columnSpan="2">
                        <Label text="Correo Electrónico *" styleClass="field-label"/>
                        <TextField fx:id="txtEmail" promptText="correo@empresa.com"
                                   styleClass="form-field"/>
                    </VBox>

                    <!-- Teléfono -->
                    <VBox spacing="5" GridPane.columnIndex="0" GridPane.rowIndex="2">
                        <Label text="Teléfono *" styleClass="field-label"/>
                        <TextField fx:id="txtTelefono" promptText="0000-0000"
                                   styleClass="form-field"/>
                    </VBox>

                    <columnConstraints>
                        <ColumnConstraints percentWidth="50"/>
                        <ColumnConstraints percentWidth="50"/>
                    </columnConstraints>
                </GridPane>
            </VBox>

            <!-- Separador decorativo -->
            <Separator style="-fx-background-color: #e0e0e0; -fx-padding: 5 0;"/>

            <!-- Sección: Información Laboral -->
            <VBox spacing="15" styleClass="form-section">
                <HBox alignment="CENTER_LEFT" spacing="10" styleClass="section-header">
                    <Label text="💼" style="-fx-font-size: 18px;"/>
                    <Label text="Información Laboral"
                           style="-fx-font-size: 16px; -fx-font-weight: bold; -fx-text-fill: #2c3e50;"/>
                    <Region HBox.hgrow="ALWAYS"/>
                    <Region style="-fx-min-width: 50; -fx-background-color: linear-gradient(to right, #9b59b6, transparent); -fx-min-height: 2;"/>
                </HBox>

                <GridPane hgap="20" vgap="18" styleClass="form-grid">

                    <!-- Cargo -->
                    <VBox spacing="5" GridPane.columnIndex="0" GridPane.rowIndex="0">
                        <Label text="Cargo *" styleClass="field-label"/>
                        <ComboBox fx:id="cmbCargo" maxWidth="Infinity" styleClass="form-field"/>
                    </VBox>

                    <!-- Fecha Contratación -->
                    <VBox spacing="5" GridPane.columnIndex="1" GridPane.rowIndex="0">
                        <Label text="Fecha de Contratación *" styleClass="field-label"/>
                        <DatePicker fx:id="dpFechaContratacion" maxWidth="Infinity" styleClass="form-field"/>
                    </VBox>

                    <!-- Estado -->
                    <VBox spacing="8" GridPane.columnIndex="0" GridPane.rowIndex="1" GridPane.columnSpan="2">
                        <Label text="Estado del Empleado" styleClass="field-label"/>
                        <HBox spacing="10" alignment="CENTER_LEFT"
                              style="-fx-background-color: #f0f0f0; -fx-padding: 12; -fx-background-radius: 8;">
                            <CheckBox fx:id="chkActivo" text="Empleado Activo"
                                      style="-fx-font-size: 14px; -fx-font-weight: bold;"/>
                            <Label text="(Puede acceder al sistema)"
                                   style="-fx-text-fill: #7f8c8d; -fx-font-size: 12px;"/>
                        </HBox>
                    </VBox>

                    <columnConstraints>
                        <ColumnConstraints percentWidth="50"/>
                        <ColumnConstraints percentWidth="50"/>
                    </columnConstraints>
                </GridPane>
            </VBox>

            <!-- Separador decorativo -->
            <Separator style="-fx-background-color: #e0e0e0; -fx-padding: 5 0;"/>

            <!-- Sección: Credenciales de Acceso -->
            <VBox spacing="15" styleClass="form-section">
                <HBox alignment="CENTER_LEFT" spacing="10" styleClass="section-header">
                    <Label text="🔐" style="-fx-font-size: 18px;"/>
                    <Label text="Credenciales de Acceso"
                           style="-fx-font-size: 16px; -fx-font-weight: bold; -fx-text-fill: #2c3e50;"/>
                    <Region HBox.hgrow="ALWAYS"/>
                    <Region style="-fx-min-width: 50; -fx-background-color: linear-gradient(to right, #e74c3c, transparent); -fx-min-height: 2;"/>
                </HBox>

                <VBox fx:id="vboxContrasena" spacing="18">

                    <GridPane hgap="20" vgap="18" styleClass="form-grid">

                        <!-- Usuario -->
                        <VBox spacing="5" GridPane.columnIndex="0" GridPane.rowIndex="0" GridPane.columnSpan="2">
                            <Label text="Nombre de Usuario *" styleClass="field-label"/>
                            <TextField fx:id="txtUsuario" promptText="usuario.sistema"
                                       styleClass="form-field"/>
                            <Label text="💡 Debe ser único en el sistema"
                                   style="-fx-text-fill: #7f8c8d; -fx-font-size: 11px; -fx-padding: 3 0 0 5;"/>
                        </VBox>

                        <!-- Contraseña -->
                        <VBox spacing="5" GridPane.columnIndex="0" GridPane.rowIndex="1">
                            <Label text="Contraseña *" styleClass="field-label"/>
                            <PasswordField fx:id="txtContrasena" promptText="Mínimo 6 caracteres"
                                           styleClass="form-field"/>
                        </VBox>

                        <!-- Confirmar Contraseña -->
                        <VBox spacing="5" GridPane.columnIndex="1" GridPane.rowIndex="1">
                            <Label text="Confirmar Contraseña *" styleClass="field-label"/>
                            <PasswordField fx:id="txtConfirmarContrasena" promptText="Repita la contraseña"
                                           styleClass="form-field"/>
                        </VBox>

                        <columnConstraints>
                            <ColumnConstraints percentWidth="50"/>
                            <ColumnConstraints percentWidth="50"/>
                        </columnConstraints>
                    </GridPane>

                    <!-- Info contraseña (solo visible en edición) -->
                    <HBox fx:id="hboxContrasenaInfo" visible="false" managed="false"
                          spacing="10" alignment="CENTER_LEFT"
                          style="-fx-background-color: #fff3cd; -fx-padding: 12; -fx-background-radius: 8; -fx-border-color: #ffc107; -fx-border-width: 1; -fx-border-radius: 8;">
                        <Label text="ℹ️" style="-fx-font-size: 16px;"/>
                        <Label fx:id="lblContrasenaInfo" text="Dejar en blanco para mantener la contraseña actual"
                               style="-fx-text-fill: #856404; -fx-font-size: 12px; -fx-font-weight: bold;"/>
                    </HBox>
                </VBox>
            </VBox>

        </VBox>
    </ScrollPane>

    <!-- Footer con botones -->
    <VBox spacing="0" style="-fx-background-color: white; -fx-effect: dropshadow(gaussian, rgba(0,0,0,0.1), 10, 0, 0, -2);">

        <!-- Nota de campos obligatorios -->
        <HBox alignment="CENTER_LEFT" spacing="8" style="-fx-padding: 15 30 10 30;">
            <Label text="*" style="-fx-text-fill: #e74c3c; -fx-font-size: 16px; -fx-font-weight: bold;"/>
            <Label text="Campos obligatorios"
                   style="-fx-text-fill: #7f8c8d; -fx-font-size: 12px; -fx-font-weight: bold;"/>
        </HBox>

        <Separator style="-fx-background-color: #e0e0e0;"/>

        <!-- Botones de acción -->
        <HBox spacing="15" alignment="CENTER" style="-fx-padding: 20 30;">
            <Region HBox.hgrow="ALWAYS"/>

            <Button fx:id="btnCancelar" text="❌ Cancelar" onAction="#handleCancelar"
                    styleClass="btn-secondary"
                    prefWidth="180" prefHeight="45"/>

            <Button fx:id="btnGuardar" text="💾 Guardar" onAction="#handleGuardar"
                    styleClass="btn-success"
                    prefWidth="180" prefHeight="45"/>
        </HBox>
    </VBox>

</VBox>